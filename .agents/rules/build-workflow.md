# Build & Development Workflow Rule

- **Do NOT run `npm run build` after every code change.**
- Focus on fast iteration and verifying functionality in the development environment first.
- Only run production builds (`npm run build`) when explicitly requested by the user or when finalizing/fixing for production deployment.
